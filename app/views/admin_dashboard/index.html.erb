<div style="font-family: 'Arial Rounded MT Bold', sans-serif">
  <h2>
    <%= "#{@current_trimester.term} - #{@current_trimester.year}" %>
  </h2>
  <ul>
    <% @current_trimester.courses.each do |course| %>
    <li>
      <%= link_to course.coding_class.title, course_path(course) %>
    </li>
    <% end %>
  </ul>
</div>

<div style="font-family: 'Arial Rounded MT Bold', sans-serif">
  <h2>
    Upcoming <%= "#{@upcoming_trimester.term} - #{@upcoming_trimester.year}" %>
  </h2>
  <ul>
    <% @upcoming_trimester.courses.each do |course| %>
    <li>
      <%= link_to course.coding_class.title, course_path(course) %>
    </li>
    <% end %>
  </ul>
</div>

